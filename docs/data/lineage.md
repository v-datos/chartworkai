# Lineage — N/A for this profile

This project (the framework's own productization) has **no raw→processed data pipeline**, so source-to-output data lineage does not apply.

The closest analog — how a consumer's filled artifacts derive from the framework's templates — is documented in `INITIALIZATION_GUIDE.md` and `framework.json`.

Retained only to satisfy the current compliance checker's required `docs/data/` triad; made profile-conditional by Phase 1 task **T-006**. See `docs/decisions/20260607_DEC002_profile_model.md`.
